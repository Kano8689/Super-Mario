using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class bgGenerator : MonoBehaviour
{
   // public GameObject bgPrefab, bgGenPoint;
    //float posX = 22.50f;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if(transform.position.x < bgGenPoint.transform.position.x)
        //{
        //    transform.position = new Vector2(transform.position.x + posX, 0f);
        //    Instantiate(bgPrefab, transform.position, Quaternion.identity);
        //}
    }
}
